<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="mybatis.r9_mybatis">
   <!-- 로그인 처리 -->
   <select id="isMember" parameterType="java.util.Map" resultType="int">
      SELECT COUNT(*) FROM R9_MEMBER WHERE emailid=#{emailid} AND pwd=#{pwd}
   </select>
   
   <!-- 회원가입 처리 -->
   <insert id="signOn" parameterType="java.util.Map">
      INSERT INTO R9_MEMBER VALUES(SEQ_R9_MEMBER.NEXTVAL, #{emailid}, #{signpwd}, #{name}, #{phonenumber}, null)
   </insert>
   
   	<!-- 중복 아이디 체크 -->
    <select id="IdCheck" parameterType="java.util.Map" resultType="int" >
       SELECT COUNT(*) FROM R9_MEMBER WHERE emailid=#{emailid}
    </select>
    
    <!-- 카카오 아이디 연동 -->
    <update id="kakaoConnect" parameterType="java.util.Map">
    	UPDATE R9_MEMBER SET kakaoid=#{kakaoid} WHERE emailid=#{emailid} AND kakaoid IS NULL
    </update>
    
    <!-- 카카오 로그인 -->
 	<select id="kakaoLogin" parameterType="java.util.Map" resultType="String">
 		SELECT emailid FROM R9_MEMBER WHERE kakaoid=#{kakaoid}
 	</select>
 	
 	<!-- 비밀번호 가져오기 -->
 	<select id="passEquals" parameterType="java.util.Map" resultType="String">
 		SELECT pwd FROM R9_MEMBER WHERE emailid=#{emailid} AND phonenumber=#{phonenumber}
 	</select>
</mapper>